import React, { Component } from "react";
const Select = () => {
  return (
    <div className="form-group">
      <label htmlFor={name}></label>
      <select name={name} id={name} className="form-control"></select>
    </div>
  );
};

export default Select;
